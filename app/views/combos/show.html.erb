<h1><%= @combo.name %></h1>
<p>Price: <%= number_to_currency(@combo.price) %></p>

<h2>Products in this Combo:</h2>
<ul>
  <% @combo.products.each do |product| %>
    <li><%= product.name %> - <%= number_to_currency(product.priceperpack) %></li>
  <% end %>
</ul>
